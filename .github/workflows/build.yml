name: Build and Release JNI Libraries

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Android NDK Libraries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: |
            aarch64-linux-android
            armv7-linux-androideabi
            x86_64-linux-android
            i686-linux-android

      - name: Rust Cache
        uses: swatinem/rust-cache@v2

      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Set Android NDK Environment
        run: |
          echo "ANDROID_NDK_HOME=$ANDROID_NDK_LATEST_HOME" >> $GITHUB_ENV

      - name: Build aarch64
        run: cargo ndk -t aarch64-linux-android build --release

      - name: Build armv7
        run: cargo ndk -t armv7-linux-androideabi build --release

      - name: Build x86_64
        run: cargo ndk -t x86_64-linux-android build --release

      - name: Build i686
        run: cargo ndk -t i686-linux-android build --release

      - name: Prepare Artifacts
        run: |
          mkdir -p out/
          cp target/aarch64-linux-android/release/libbbf.so out/libbbf-aarch64.so
          cp target/armv7-linux-androideabi/release/libbbf.so out/libbbf-armv7.so
          cp target/x86_64-linux-android/release/libbbf.so out/libbbf-x86_64.so
          cp target/i686-linux-android/release/libbbf.so out/libbbf-x86.so

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: libbbf-android-libs
          path: out/*.so

  create_release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          name: libbbf-android-libs
          path: out

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: out/*.so
          generate_release_notes: true